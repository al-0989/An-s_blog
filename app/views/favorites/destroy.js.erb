// Here we just continue to render the post-fav partial as it takes care of all the logic for us already. If we were doing something like removing a comment or post we would have instructions for removing it from the html/page.
console.log("made it to the destroy.js");
$("#post-fav").html("<%= j render 'post_fav' %>");
